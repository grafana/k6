diff --git a/WebCryptoAPI/sign_verify/ecdsa.js b/WebCryptoAPI/sign_verify/ecdsa.js
index 6bf662adc..7a1eb0178 100644
--- a/WebCryptoAPI/sign_verify/ecdsa.js
+++ b/WebCryptoAPI/sign_verify/ecdsa.js
@@ -1,8 +1,8 @@
 
 function run_test() {
-    setup({explicit_done: true});
+    // setup({explicit_done: true});
 
-    var subtle = self.crypto.subtle; // Change to test prefixed implementations
+    var subtle = crypto.subtle; // Change to test prefixed implementations
 
     // When are all these tests really done? When all the promises they use have resolved.
     var all_promises = [];
@@ -435,12 +435,6 @@ function run_test() {
         all_promises.push(promise);
     });
 
-    promise_test(function() {
-        return Promise.all(all_promises)
-            .then(function() {done();})
-            .catch(function() {done();})
-    }, "setup");
-
     // A test vector has all needed fields for signing and verifying, EXCEPT that the
     // key field may be null. This function replaces that null with the Correct
     // CryptoKey object.
