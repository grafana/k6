diff --git a/WebCryptoAPI/encrypt_decrypt/aes.js b/WebCryptoAPI/encrypt_decrypt/aes.js
index fdeb7963f..b2257a320 100644
--- a/WebCryptoAPI/encrypt_decrypt/aes.js
+++ b/WebCryptoAPI/encrypt_decrypt/aes.js
@@ -267,12 +267,6 @@ function run_test() {
         all_promises.push(promise);
     });
 
-    promise_test(function() {
-        return Promise.all(all_promises)
-            .then(function() {done();})
-            .catch(function() {done();})
-    }, "setup");
-
     // A test vector has all needed fields for encryption, EXCEPT that the
     // key field may be null. This function replaces that null with the Correct
     // CryptoKey object.
