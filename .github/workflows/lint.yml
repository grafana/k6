name: Lint
on:
  # Enable manually triggering this workflow via the API or web UI
  workflow_dispatch:
  pull_request:
    paths-ignore:
      - 'docs/**'
      - 'release notes/**'

defaults:
  run:
    shell: bash

permissions:
  contents: read

jobs:
  generate-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
          fetch-depth: 0
      - name: Resolve Go versions
        id: go_versions
        uses: ./.github/actions/go-versions
      - name: Install Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6
        with:
          go-version: ${{ steps.go_versions.outputs.current }}
          check-latest: true
      - name: Check code generation
        run: |
          make generate
          if [ -n "$(git status --porcelain)" ]; then
            echo "make generate produced changes:"
            git status --porcelain
            git --no-pager diff
            exit 1
          fi

  k6-ci:
    uses: grafana/k6-ci/.github/workflows/all.yml@777962b022a831166517639e542630d8b7d1455f # v0.4.0
    with:
      k6-ci-ref: 777962b022a831166517639e542630d8b7d1455f # v0.4.0
      skip-tests: true
      skip-extension-testing: true
