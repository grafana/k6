name: Publish TechDocs
on:
  push:
    branches:
      - master
    paths:
      - 'docs/docs/**'
      - 'docs/mkdocs.yml'
      - 'catalog-info.yaml'
      - '.github/workflows/publish-techdocs.yaml'
concurrency:
  group: '${{ github.workflow }}-${{ github.ref }}'
  cancel-in-progress: true
permissions: {}
jobs:
  publish-docs:
    uses: grafana/shared-workflows/.github/workflows/publish-techdocs.yaml@3adde9659883b3cd0c65d6acaa356b78a5f8f7f4 # main
    permissions:
      contents: read
      id-token: write
    with:
      namespace: default
      kind: component
      name: k6
      default-working-directory: .
