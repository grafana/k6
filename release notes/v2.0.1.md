k6 `v2.0.1` is here 🎉! This release includes:

- Dependency updates for `golang.org/x/net`.

## Maintenance and internal improvements

- Updates `golang.org/x/net` to `v0.55.0` [security] (CVE-2026-39821; fixed in `v0.54.0`).
